@using ExamPortal.BusinessLogic.ViewModel.Examintaion
@model AddExamViewModel

<partial name="_ValidationScriptsPartial" />
<div class="modal fade" id="editExamModal" tabindex="-1" aria-labelledby="editExamLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content rounded-4 shadow">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="addExamLabel"><i class="bi bi-journal-plus me-2"></i>Edit Exam</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form method="post" id="editExamForm" asp-controller="Examination" asp-action="EditExam">
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">Title</label>
                            <input type="hidden" asp-for="Id">
                            <input asp-for="Title" class="form-control" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Start Date</label>
                            <input id="startDateEdit" type="datetime-local" asp-for="StartDate" class="form-control"
                                action="" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">End Date</label>
                            <input id="endDateEdit" type="datetime-local" asp-for="EndDate" class="form-control" />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Duration (Minutes)</label>
                            <input type="number" asp-for="Duration" class="form-control" />
                            <span asp-validation-for="Duration" class="text-danger"></span>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Description</label>
                            <textarea asp-for="Description" class="form-control" rows="3" placeholder=""></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                        <div class="col-12">
                            <label class="form-label">Assign to Students</label>
                            <div class="dropdown w-100">
                                <button
                                    class="btn btn-outline-secondary dropdown-toggle w-100 text-start d-flex justify-content-start align-items-center"
                                    type="button" id="studentDropdownEdit" data-bs-toggle="dropdown"
                                    aria-expanded="false">
                                    <span class="me-1" id="dropdownButtonTextEdit">Select Students</span>
                                    <span class="badge bg-primary rounded-pill" id="selectedCountBadgeEdit"
                                        style="display: none;">0</span>
                                </button>
                                <div class="dropdown-menu w-100 p-0" style="max-height: 300px; overflow-y: auto;">
                                    <div class="dropdown-item-text border-bottom bg-light">
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" id="selectAllStudentsEdit">
                                            <label class="form-check-label fw-bold text-primary"
                                                for="selectAllStudentsEdit">
                                                <i class="bi bi-people-fill me-2"></i>Select All Students
                                            </label>
                                        </div>
                                    </div>
                                    <div id="studentDropdownListEdit">
                                        @foreach (var student in Model.Students)
                                        {
                                            var isSelected = Model.SelectedStudentIds != null &&
                                            Model.SelectedStudentIds.Contains(student.Id);
                                            <div class="dropdown-item-text student-dropdown-item-edit"
                                                data-student-id="@student.Id"
                                                data-student-name="@student.FirstName @student.LastName">
                                                <div class="form-check">
                                                    <input class="form-check-input student-checkbox-edit" type="checkbox"
                                                        name="SelectedStudentIds" value="@student.Id"
                                                        id="student_edit_@student.Id" @(isSelected ? "checked" : "")>
                                                    <label class="form-check-label w-100" for="student_edit_@student.Id">
                                                        <div class="d-flex justify-content-between align-items-center">
                                                            <div>
                                                                <div class="fw-medium">@student.FirstName @student.LastName
                                                                </div>
                                                                <small class="text-muted">
                                                                    @if (!string.IsNullOrEmpty(student.Email))
                                                                    {
                                                                        @student.Email
                                                                    }
                                                                </small>
                                                            </div>
                                                            <i class="bi bi-person-circle text-muted"></i>
                                                        </div>
                                                    </label>
                                                </div>
                                            </div>
                                        }
                                    </div>
                                </div>
                            </div>
                            <span asp-validation-for="SelectedStudentIds" class="text-danger"></span>
                        </div>

                    </div>
                </div>
                <div class="modal-footer border-top-0">
                    <button type="submit" class="btn primary-button-v1">
                        <i class="bi bi-save2-fill me-1"></i>Save and Continue
                    </button>
                    <button type="button" class="btn secondary-button" data-bs-dismiss="modal">
                        Cancel
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
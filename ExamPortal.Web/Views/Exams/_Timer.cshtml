@{
    var minutes = 500 / 60;
    var seconds = 512 % 60;
}

<div class="alert alert-dark d-flex align-items-center">
    <i class="bi bi-clock me-2"></i>
    <div>
        <strong>Time Remaining:</strong> @minutes:@seconds.ToString("D2")
    </div>
</div>
